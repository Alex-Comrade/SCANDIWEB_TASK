import { useQuery } from "@apollo/client";

const CartModal = () => {
    return (
        <div>
            <img src='$' alt='$' />
            <p></p>
            <div></div>
        </div>
    );
};

export default CartModal;
